services:
  - type: web
    name: trippilot-pro
    runtime: node
    buildCommand: npm install && npm run build:server
    startCommand: npm run start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
